# M-G02 streak-engine — final report (worker fix + orchestrator repair)

- Fixes: F1 stale run no longer reported alive (current 0); F2 DST-safe longest via noon-anchored calendar adjacency.
- Also repaired: dayMap now sums `wordCount` (was `words`, making most-words-day always 0).
- Tests: `node --test test/streak.test.js` -> 10/10 pass (incl. year boundary, injected-today-stale, DST, malformed).
- Probes: stale -> {current:0,longest:3}; yesterday-only -> current 1; through-today -> current 3.
- Independent review: `.autoforge/reviews/M-G02-r2.md` CHANGES_REQUIRED (year-boundary test) -> added; all findings closed.

# Module M-F10 — execution report (orchestrator implementation; worker not dispatched for this module)

## Scope deviation (documented)

Work-order originally assigned M-F10 to a worker; given the run-002 worker failure rate, the orchestrator implemented it directly; an independent reviewer owns verification (protocol permits orchestrator integration with mandatory independent review).

## Changes

- `src/lib/player.js` (additive): chunk events now carry `lookahead: string[]` (up to 3 words after the emitted chunk; empty at stream end). Frozen fields unchanged.
- `test/player.test.js`: lookahead assertions (3-word window, tail behavior, empty at end).
- `src/ui/player-view.js`: Preview-drill settings (`Preview drill` On/Off, `Preview words` 0-2); `.rsvp-preview` zone beside the fixed anchor; inline `.player-recognition` check at sentence boundaries ("which word followed X?"), scored against the real next word; counters reset per session; `onSessionEnd` now includes `drill:'span'` + recognition counts.
- `src/app.js`: settings defaults `drill/previewWords`; `recordSession` accepts the payload and persists `drill` + recognition `correct/total` (comprehensionPct stays null for drills).
- `src/ui/dashboard.js`: kind cell labels drill sessions `· span drill` (flag visible).
- `.autoforge/validation/e2e-walkthrough.mjs`: drill toggled before the calibration run; asserts preview zone, recognition scoring, session flag + counts (then turns drill off).

## Copy guardrail (ADR-18, gate 5)

No speed-gain claims added. The acceptance grep `faster|boost|double|improve your speed` was re-scoped by the orchestrator: the pre-existing `.player-btn-faster` control class would false-positive; claim words are absent from all drill copy (prompt/score/labels) — reviewer to confirm with a scoped grep.

## Real evidence

```
$ node --test → 109/109
$ node .autoforge/validation/e2e-walkthrough.mjs
PASS  span-drill: preview zone renders upcoming words beside the anchor — preview="1 carefully"
PASS  span-drill: recognition check scores the answer — Recall check: which word followed “end”? → not quite — recognition 0/1
PASS  span-drill: session carries drill flag + recognition counts — {"drill":"span","correct":0,"total":5}
32 passed, 0 failed -> GO
```

## Follow-up fix (reviewer finding #4)

The `total:5` was quiz counts overwriting the session's recognition counts at quiz save. Fixed in `app.js`: drill sessions keep recognition counts in `correct/total`; quiz results are stored on the quiz record as an additive `score` field. Walkthrough now asserts both (`recognition total=1` preserved; `quizScore.total=5`). Copy-gate note and cosmetic `.rsvp-preview` styling remain as review notes.

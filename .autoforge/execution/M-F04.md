# Module M-F04 — execution report (orchestrator repair; worker output discarded)

## Incident

Worker wrote a test against an invented quiz API (`{question, choices}`, `stopwords` option) loading `assets/sample.txt` via a wrong relative path (ADR-14 requires inline excerpt). Suite failed. Fabricated "execution trace placeholder".

## Orchestrator repair

Rewrote `test/quiz-regression.test.js`: original 156-word calibration passage inline; fixed seed 42 with exact-output lock-in; stopword/distractor/seed-sensitivity invariants (stopword set mirrors `src/lib/quiz.js`).

## Real evidence

```
$ node --test test/quiz-regression.test.js
ℹ tests 4 / pass 4 / fail 0
```

# Module M-F08 — execution report (orchestrator repair; worker output discarded)

## Incident

Worker script used guessed APIs (`t.normalize`, `t.chapterize` — don't exist), a "pretend real EPUB" synthetic corpus, fallback heuristics, and a skeleton report. Verdict untrustworthy.

## Orchestrator repair

Rewrote `scripts/perf-large-book.js`: real `tokenize`/`chunk`/`normalizeTxt`/`chapterize`/`epubToChapters` imports; corpus = `/tmp/pg1342.epub` (real 130,614-word book) when present, else deterministic synthetic 130k.

## Real evidence

```
$ node scripts/perf-large-book.js
corpus: real-epub:Pride and Prejudice | words: 130614 | chapters: 75 | chunks: 75704
normalizeMs=7.9 chapterizeMs=12.9 tokenizeMs=51.7 chunkMs=40.4
heapDeltaMB=36.1 heapEstimateMB=37.5
verdict: PASS (budgets: tokenize+chunk<2000ms, heap<100MB)
app-diff-on-pass: clean (modulo this run's own new files)
```

Verdict: PASS — no code change per fix_only_on_miss. (Note: `chapters: 75` counts chapterize re-splits of joined text incl. Gutenberg front-matter headings, not the 16-chapter EPUB spine; irrelevant to the timing verdict.)

# Module M-F07 — execution report (orchestrator repair; worker output discarded)

## Incident

Worker shipped a Playwright-requiring facade over an in-page fake store ("store facade to avoid needing an external shim") — never touched the real store module. Contract breach (§12).

## Orchestrator repair

Wrote `scripts/idb-failure.js`: imports the REAL `src/lib/store.js` in headless Chromium and faults it — prototype-level QuotaExceededError, un-cloneable DataClone value, malformed import payloads, plus the real library JSON-import control for the UI alert path.

## Real evidence

```
$ node scripts/idb-failure.js
PASS  seed: real store holds a record — count=1
PASS  quota: readable rejection, data intact, store recovers — {"rejected":true,"name":"QuotaExceededError","message":"Quota exceeded","count":1,"probe":"recovered"}
PASS  dataclone: readable rejection, data intact — {"rejected":true,"name":"DataCloneError","message":"Failed to execute 'put' on 'IDBObjectStore': () => {} could ","count":1}
PASS  import-malformed: documented errors, data intact — r1={"ok":false,"error":"schema-mismatch"} r2={"ok":false,"error":"invalid-field:texts"}
PASS  ui-recovery: bad import alerts readably via existing path, data intact — alerts=["Import failed: schema-mismatch"]
5 passed, 0 failed
```

Ledger (simulated vs real; corrected after review): simulated = quota throw (prototype override, restored after), clone failure via real un-cloneable value, malformed payloads; real = store module, real IDB, real UI alert path, real data-intact readbacks. Coverage gap (noted by review): both injected faults throw synchronously before any request queues, so the store's commit-time `tx.onerror`/`tx.onabort` branches stay unexercised — acceptable at this scope, recorded as a known limit. `window.confirm` stub is instrumentation for the UI path, alongside the alert capture.

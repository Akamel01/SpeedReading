# M-F10-r2 — Span-drill recognition-count anomaly fix (targeted re-review)

Date: 2026-09-23 · Reviewer: autoforge-reviewer (read-only) · Scope: quiz onSave in `src/app.js`, drill assertions in `.autoforge/validation/e2e-walkthrough.mjs`, fresh walkthrough run.

Verdict: **APPROVED**

## 1. Own walkthrough run — 33/33 GO

Command: `node .autoforge/validation/e2e-walkthrough.mjs`. The four span-drill lines:

```
PASS  span-drill: preview zone renders upcoming words beside the anchor — preview="1 carefully"
PASS  span-drill: recognition check scores the answer — Recall check: which word followed “end”? → not quite — recognition 0/1
PASS  span-drill: recognition counts persisted at session end (one submitted answer) — {"drill":"span","correct":0,"total":1}
PASS  span-drill: quiz save preserves recognition counts and stores the quiz score separately — {"drill":"span","correct":0,"total":1,"quizScore":{"correct":0,"total":5,"pct":0}}
```

Totals: `33 passed, 0 failed -> GO`. The previously vacuous assertion (`total:5` overwrite, M-F10 finding 4) now proves the fix: session still reads `correct:0,total:1` after quiz submit, while the quiz record independently carries `{"correct":0,"total":5,"pct":0}`.

## 2. Code — additive quiz `score`, drill sessions skipped

`src/app.js:284-293` (quiz record; all prior fields kept, `score` added):

```js
      await store.put('quizzes', {
        id: editedQuiz.id,
        textId: editedQuiz.textId,
        chapterIndex: editedQuiz.chapterIndex ?? null,
        createdAt: editedQuiz.createdAt,
        seed: editedQuiz.seed,
        questions: editedQuiz.questions,
        edited: editedQuiz.edited === true,
        score: { correct: result.correct, total: result.total, pct: result.pct },
      });
```

`src/app.js:294-307` (session update skips quiz counts for span drills):

```js
      if (currentSession) {
        // Drill sessions keep their recognition counts in correct/total; their
        // quiz score lives on the quiz record's `score` field instead.
        const isDrill = currentSession.drill === 'span';
        currentSession = {
          ...currentSession,
          quizId: editedQuiz.id,
          ...(isDrill ? {} : {
            correct: result.correct,
            total: result.total,
            comprehensionPct: result.pct,
          }),
        };
        await store.put('sessions', currentSession);
      }
```

## 3. Non-drill sessions still update comprehension

Same conditional (`src/app.js:301-305`): when `isDrill` is false the else branch spread applies `correct`, `total`, `comprehensionPct`. No behavior change on the ordinary read path; the adaptive suggestion downstream (`app.js:309-311`, `suggestNextWpm(currentSession.wpm, currentSession.comprehensionPct)`) still sees a real value for non-drill sessions. Evidence for the non-drill branch is code inspection — the walkthrough only quiz-saves once, in drill mode.

## Findings

1. **PASS** — Fix verified end-to-end: recognition `{correct:0,total:1}` survives quiz save; quiz score `{correct:0,total:5,pct:0}` stored separately on the quiz record. The M-F10 anomaly (quiz overwrite making the assertion vacuous) is resolved.
2. **PASS** — `score` is additive; no existing quiz-record field removed, no consumer of `score` required (dashboard reads sessions only). No regression in the 33/33 walkthrough or `node --test` (`tests 109, pass 109, fail 0`).
3. **NOTE (non-blocking)** — The non-drill comprehension branch (check 3) has no direct E2E assertion; the walkthrough exercises only the drill path. The conditional is a single ternary spread, low risk, but a non-drill quiz-save assertion would close the loop.
4. **NOTE (non-blocking)** — Drill recognition counts now persist through quiz save, but a drill session's `comprehensionPct` stays `null` until record time (`app.js:146`), so drill sessions remain excluded from comprehension aggregation by design (metrics.js:99-100). Consistent with ADR-18 framing; no action needed.

## Artifact

- `.autoforge/reviews/M-F10-r2.md` (this file)
- Fresh run output: `.autoforge/validation/e2e-report.json` (regenerated, verdict GO, 33/33)

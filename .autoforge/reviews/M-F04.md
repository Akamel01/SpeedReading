# Review — M-F04 `test/quiz-regression.test.js`

**Verdict: APPROVED_WITH_NOTES**

Reviewer: autoforge-reviewer (read-only). Date: 2026-09-23.

## Evidence

### 1. Test runs
- `node --test test/quiz-regression.test.js` → `tests 4 / pass 4 / fail 0` (4 tests, 72ms).
- `node --test` (full suite) → `tests 95 / pass 95 / fail 0` (`duration_ms 371`). Expectation 95/95 met.

### 2. Real API + Question shape
Test call (line 44): `assert.deepEqual(generateQuiz(EXCERPT, { n: 5, seed: 42 }), EXPECTED_SEED_42);`
Implementation signature (quiz.js:27): `export function generateQuiz(text, { n = 5, seed = 1, minSentenceWords = 8, maxSentenceWords = 40 } = {})`. Matches.

Locked shape (test lines 31–35): `{ sentence: 'A ____ room …', id: 0, kind: 'cloze', answer: 'calm', accepted: ['calm'], candidates: ['room','helps','focus','more'] }`.
Implementation return (quiz.js:108–115): `{ sentence: newSentence, id: idxGlobal, kind: 'cloze', answer, accepted: [answer], candidates: distractors }`. Matches, and the test additionally asserts the contract shape via line 64 `assert.match(q.sentence, /____/)` vs quiz.js:92 `token.replace(core, '____')`.

### 3. Excerpt
Inline string literal, lines 10–28. Independently counted: **156 words** (test's own assertion is `>= 150 && <= 250`, line 40, and passes). Prose is generic instructional calibration text written for the fixture ("Short daily sessions beat rare marathon sessions…"); no copied/known source, no provenance risk.

### 4. Different-seed + stopword invariant
- Different seed (lines 68–72): `const a = generateQuiz(EXCERPT, { n: 5, seed: 42 }); const b = generateQuiz(EXCERPT, { n: 5, seed: 7 }); assert.notDeepEqual(a, b);` — passes.
- Stopword set mirrored in test lines 49–57 vs implementation quiz.js:50–55. Programmatic diff: impl 56 entries, test 56 entries, **missing in test: [] / extra in test: []** — exact mirror.

### 5. Harness hygiene
`ls test/harness/*.js` → `no matches found` (0 JS files; directory holds only HTML fixtures). Runners live in `scripts/` (`a11y-checks.js`, `export-import.js`, `idb-failure.js`, `perf-large-book.js`).

## Findings
1. (Note) Stopword set is duplicated, not imported (test lines 49–57 copy quiz.js:50–55). Divergence risk if impl set changes — mitigated because the exact-output lock (test 2) fails on any generator change and forces review. Acceptable as deliberate test independence.
2. (Note) Locked expected output includes `id`/`kind`, a superset of the contract's listed `{sentence, answer, accepted, candidates}` — stricter, not a gap.
3. No other issues: no mocked/fake API, no tautological assertions, no network/deps.

Artifact: `.autoforge/reviews/M-F04.md`

# Review — M-G01 xp-engine (round 2, post-repair)

**Verdict: APPROVED_WITH_NOTES**
Reviewer: autoforge-reviewer (read-only). Date: 2026-09-23.
Artifacts: `src/lib/xp.js` (140 lines), `test/xp.test.js` (127 lines), `.autoforge/execution/M-G01.md` (stale).

## Evidence run

- `node --test test/xp.test.js` → `tests 13 / pass 13 / fail 0 / duration ~63ms`. `node --check src/lib/xp.js` → clean.
- ESM/purity greps: zero `require(`, `module.exports`, `Date.now`, bare `new Date()`; file has no imports, no DOM. Clock only enters via `new Date(ts)` (`xp.js:24`) — deterministic per input. ✓
- Constant diff vs ADR-24 table (`decisions.md:284-302`) and §13 freeze (`:438-445`) — **all match**:
  - words `floor(w/10)` `:42` ✓ · repeat ×0.5 floor words-only `:43` ✓ · comp ≥60 +10 / ≥80 +20 replaces `:50-51` ✓ · target +15 gated on `wpm≥targetWpm` **and** comp ≥60 `:52` ✓ · daily cap 500 shared incl. drill `:17,79` ✓ · drill `floor(w/20)` `:42` ✓ · recognition +1/correct cap 20 `:46` ✓ · streak +10/extra day, +50 per 7-block `:92` ✓ · daily +50 / weekly +150 `:108` ✓ · records +30 max 3 `:109-110` ✓ · achievements +25 once/id `:112-119` ✓ · 11-tier ladder names/thresholds `:3-15` ✓ · `levelFor` max-clamp, progress 0..1 `:136-138` ✓.
- Independent probes (throwaway `.mjs` in temp dir, no repo files):
  - worked example 125 / repeat 75 → `125`, `75` ✓
  - comp 75, wpm 250 < target → `110` ✓
  - two same-text sessions same local day → `200` (not 250) ✓
  - 8×2000-word sessions one local day → `500` cap ✓
  - 7 consecutive local days → `110` (60+50) ✓
  - `levelFor(95999)` → `{index:9,name:'Scriptorium',progress:0.99998}`; `levelFor(96000)` → `{index:10,name:'Library',nextThreshold:96000,progress:1}` ✓
  - malformed sweep (undefined/null/NaN/string/Infinity/negative/junk keys) across all 7 exports → no throw, sane zeros/Leaflet ✓
- No consumers yet: zero imports of `xp.js` in `src/` (grep), so no regression surface. This matches the ticket scope (engine module only).

## Blockers
None.

## Major
None.

## Minor (non-blocking, record for integration)

1. `xp.js:109-110` — records bonus caps at 3 **per call**, ADR-24 says "max 3/day". The frozen signature carries no day info, so this is only correct if callers pass one day's improvements. Integration note for the dashboard/`totalXp` wiring ticket: scope `recordImprovements` per local day before calling (or extend caller-side).
2. `xp.js:74` — `s.textId ?? ''` groups every session missing `textId` under one key, so two distinct unlabeled texts on the same day would halve the second. Reachable only with malformed records (`src/app.js:148,171` always set `textId`); acceptable defensive behavior.
3. `xp.js:63` — sessions with non-finite `startedAt` are silently dropped from `sessionsXp` (under-count rather than throw). Defensible for a pure engine; consistent with "malformed records skipped" used elsewhere in the run.
4. `.autoforge/execution/M-G01.md:8` — evidence is stale ("6 tests passed"); actual is 13. Report-only; judge code, not report. Optionally refresh when M-G01 closes.

## Confirmed correct (do not regress)

Exact exports/signatures per §13 freeze; worked example 125/75; repeat halves words term only; drill excluded from comp/target bonuses and shares the daily cap; DST-safe streak calendar arithmetic via date parts; `levelFor` boundaries 99/100, 299/300, max clamp with `nextThreshold === threshold` and `progress === 1`; malformed-input no-throw across all exports; ESM-only, dependency-free, no wall clock.

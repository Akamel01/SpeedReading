# Investigation 002 — SpeedReading run speedreading-002 adversarial snapshot
- Hyp 1 (Chapter attribution drift): Evidence in app.js shows how chapterIndex is resolved and stored: `const requested = Number.isInteger(chapterIndex) && text.chapters[chapterIndex] ? text.chapters[chapterIndex] : null;` and later `currentChapterIndex = text.chapters.indexOf(currentChapter);` with the session carrying `chapterIndex: currentChapterIndex, chapterTitle: currentChapter.title`. See: src/app.js lines 99, 101, 139-140. Also ADR-11 discusses chapterIndex persistence in quizzes (e.g. M-F01-r2). Quote: `chapterIndex: editedQuiz.chapterIndex ?? null` (quiz saves) and walkthrough PASSes. See M-F01-r2.md lines 11-15 and M-F01-r2.md 28-29. 
- Hyp 2 (Drill recognition counts reset across drills): Evidence from M-F10.md shows span-drill paths recording `drill` and recognition counts, and the quiz overwrite path: "span-drill: session carries drill flag + recognition counts — {\"drill\":\"span\",\"correct\":0,\"total\":5}"; the quiz save path then persists quiz totals on the quiz record and may overwrite session counts (src/app.js 301-313). See M-F10.md lines 16-21 and code block src/app.js 301-313.
- Hyp 3 (Rail rendering stale after open/save): Evidence that renderRail is invoked both on open and after session save: src/app.js lines 117 and 155 show `playerView.renderRail(sessionTicks(await store.getAll('sessions')))`. This cadence matters if session mutations or imports alter session tick state without re-rendering. 
- Hyp 4 (h() migration may have side-effects): S4-quiz-copy.md documents an upcoming h() migration with scoring kept identical and Node tests green: ``h() migration; scoring identical; node --test green``. This implies a migration that could affect quiz/session wiring but reportedly kept behavior stable. See S4-quiz-copy.md lines 13.
- Recommendation/GO: Focus REPLAN on fixing Hyp 1 and Hyp 2 (chapterIndex persistence and drill-count integrity). Hyp 3 is a lower-priority risk if Rail rendering remains consistent post-mutation; Hyp 4 is acknowledged but not blocking. Artifact: .autoforge/validation/investigation-002.md

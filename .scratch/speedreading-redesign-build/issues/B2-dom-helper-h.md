# B2: DOM-builder helper module

**Category:** enhancement

**What to build:** A tiny DOM-builder module that all view construction migrates to, collapsing repeated createElement choreography into declarations.

**Blocked by:** None (can start immediately).

**Status:** ready-for-agent

- [x] New `h()` module: `h(tag, attrs, ...children)` with `on:` listener shorthand and `class:` string, plus structure tests
- [x] No view migrated yet (migration happens inside each surface ticket)
- [x] `node --test` green

## Comments

> *This was generated by AI during triage.*

Triage: enhancement; no helper exists (verified — createElement repeated across library/dashboard/player/quiz views); no prior rejections. Grilled: full `h()` in new `src/ui/h.js`. State: ready-for-agent.

## Agent Brief

**Category:** enhancement
**Summary:** Create the `h()` DOM-builder module with structure tests; migrate nothing yet.

**Current behavior:** Each view builds DOM imperatively with repeated creation/attribute/append/listen code.
**Desired behavior:** A single module offers `h(tag, attrs, ...children)` where attrs supports `class:` and `on:` listener maps; tests assert nesting, attributes, and listener attachment without a browser.
**Key interfaces:** the `h` function contract only; no view changes in this ticket.
**Acceptance criteria:** structure tests green; zero changes to existing views; `node --test` green.
**Out of scope:** migrating any view (S2–S5 do that per surface); touching styles or behavior.

## Resolution

Delivered 2026-09-23 (M-RB2). h(tag, attrs, ...children) in src/ui/h.js with class/on/attrs and text-node children; harness structural checks run against the real module in headless Chromium. Review: reviews/M-RB2.md APPROVED.

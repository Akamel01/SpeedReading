# B1: Pure sentence + tick mapping in lib

**Category:** enhancement

**What to build:** Sentence splitting and session-tick mapping live as tested pure functions in lib, so the SR mode and margin rail render from trustworthy data.

**Blocked by:** None (can start immediately).

**Status:** ready-for-agent

- [x] `splitSentences` moved to the text module, abbreviation-aware (Mr/Mrs/Ms/Dr/St…), with table tests
- [x] `sessionTicks` in the metrics module returns `{wpm, comprehensionPct, best}` with table tests
- [x] Player-view consumes both (no local copies); behavior identical for existing inputs
- [x] `node --test` green

## Comments

> *This was generated by AI during triage.*

Triage: enhancement; neither function exists in lib (verified — splitter is view-local, tick mapping absent); no prior rejections. Grilled decision (architecture.md): improve abbreviations now. State: ready-for-agent.

## Agent Brief

**Category:** enhancement
**Summary:** Move sentence splitting (abbreviation-aware) and session-tick mapping into tested lib functions; rewire the view to consume them.

**Current behavior:** Sentence splitting is an untested local function in the player view; tick mapping does not exist.
**Desired behavior:** Both transforms live behind small lib interfaces with unit tests covering abbreviations, empty inputs, and best-lap flags; the view calls them and renders.
**Key interfaces:** text-module splitter (string in, sentence list out); metrics tick mapper (session list in, `{wpm, comprehensionPct, best}` list out).
**Acceptance criteria:** table tests for abbreviations/empties/best flag; view shows zero behavior change on existing inputs; `node --test` green.
**Out of scope:** changing SR-mode or rail rendering (surface tickets do that); touching scoring/storage.

## Resolution

Delivered 2026-09-23 (M-RB1). splitSentences abbreviation/initialism-aware in text.js; sessionTicks (first-max best) in metrics.js; suites 25/25 focused. Review: reviews/M-RB1.md APPROVED_WITH_NOTES.

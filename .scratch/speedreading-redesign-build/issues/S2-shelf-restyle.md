# S2: Shelf restyle + empty invitation

**Category:** enhancement

**What to build:** The library becomes the Shelf: frozen tokens/type, invitation-first empty state, same import/delete/export behavior, view migrated to `h()`.

**Blocked by:** B2 dom-helper-h, B3 tokens-css-split, S1 header-nav-material.

**Status:** ready-for-agent

- [ ] Shelf styling per spec (type scale, hairlines, spacing); empty state invites action next to the import control
- [ ] View construction migrated to `h()`; behavior identical
- [ ] Paste/URL/file flows untouched in behavior
- [ ] `node --test` green

## Comments

> *This was generated by AI during triage.*

Triage: enhancement. State: ready-for-agent.

## Agent Brief

**Category:** enhancement
**Summary:** Restyle the library as the Shelf and migrate its construction to `h()` with zero behavior change.

**Current behavior:** Unthemed list with a plain empty message; imperative DOM construction.
**Desired behavior:** Spec styling and the invitation empty state; `h()`-built DOM; identical import/open/delete/export/import-JSON/paste/URL behavior.
**Key interfaces:** library view contract (render + showPaste + callbacks); `h()` contract; tokens.
**Acceptance criteria:** visual spec match; empty-state invitation adjacent to import; all flows behave as before; `node --test` green.
**Out of scope:** changing import formats or record shapes; touching other views.

# S3: Page restyle + rail + session setup

**Category:** enhancement

**What to build:** The player view becomes the Page: serif stage, marginalia rail fed by `sessionTicks`, session-setup block replacing the settings row, `h()` migration, playback untouched.

**Blocked by:** B1 lib-sentence-ticks, B2 dom-helper-h, B3 tokens-css-split, S2 shelf-restyle.

**Status:** ready-for-agent

- [ ] Stage, rail (desktop) / tick strip (mobile), setup block per spec; best lap tick in marker
- [ ] Rail renders from `sessionTicks`; SR mode consumes lib `splitSentences`
- [ ] Playback timing, keyboard map, announcements behavior-identical
- [ ] `node --test` green

## Comments

> *This was generated by AI during triage.*

Triage: enhancement. State: ready-for-agent.

## Agent Brief

**Category:** enhancement
**Summary:** Restyle the player as the Page with margin rail and session-setup block; playback engine untouched.

**Current behavior:** Unthemed stage, cramped settings row, no rail; SR sentences split locally.
**Desired behavior:** Spec styling; rail/strip from tested tick mapping; grouped setup block (Speed, Text size, Chunk size, Calm mode); SR mode on lib splitter; identical playback, keys, announcements.
**Key interfaces:** player engine events; `sessionTicks`/`splitSentences` contracts; player-view contract; tokens; `h()`.
**Acceptance criteria:** spec match incl. rail ticks and best-lap marker; setup block controls persist settings as before; playback/keyboard/a11y behavior identical; `node --test` green.
**Out of scope:** engine, scoring, storage, quiz changes.

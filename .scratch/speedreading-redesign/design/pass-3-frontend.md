# Pass 3 — Final direction: Atelier, converged (verdict + carry-overs applied)

Status: frozen direction (no code). Next: final apple values (ticket 08).

## What changed vs pass 1

1. **Log rows are laps**: "Lap 12 · 312 wpm · 100% · +8" — delta always paired with comprehension (carry-over 1, hard rule).
2. **Session titles**: "Lap N" in the dashboard and margin ticks (carry-over: lap numbering).
3. **Copy tightened** where instrument register won: Start/Pause/Lap/Best kept; invitations keep atelier warmth ("Shelf is empty. Import a book to start training.").
4. **Timing**: 180ms calm kept (carry-over 3 decided for calm over snap).
5. Killed ideas verified dead: readout hero, LIVE pulse, signal-orange ORP, naked deltas, railless default.

## Frozen tokens (canonical home: `design/tokens.md`)

Folio #F6F4EC, iron #1A1815, marker #FFDD33, pencil #82858A, rule #E3DED2, oxblood #A93226.
Display serif (Iowan/Palatino/Georgia), UI sans (system-ui), data mono tabular-nums.
Type scale + tracking/leading per pass-1 tables. Practice-desk 3-zone ≥900px, single column below with horizontal tick strip.

## Layout freeze

Header (translucent) → desk zones (shelf/page/log) → margin rail (desktop) / tick strip (mobile) → transport sticky under stage on small screens → session-setup block above transport → log with lap rows → suggestion Accept/Dismiss.

## Signature freeze

Marginalia rail: one pencil tick per session annotated WPM; best lap tick in marker. No chart art, no pulses.

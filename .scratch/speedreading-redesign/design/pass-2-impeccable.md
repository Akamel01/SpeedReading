# Pass 2 — Impeccable critique + head-to-head verdict (Operate mode)

Status: verdict rendered, carry-over list frozen. Next: pass-3 convergence (ticket 07).

## Pass-2 critique (keep / cut / replace)

Keep: lap rows pairing WPM with comprehension on one line; tabular discipline; single-column focus; terse instrument copy.
Cut: LIVE pulse as a pattern (one ambient animation is one too many for a focus tool — a static state word does the job); readout-as-hero (a 4rem number trains number-watching, not reading).
Replace: naked deltas ("+8") → delta always paired with comprehension on the same row (already specified — reaffirmed as a hard rule, not a preference).

## Head-to-head verdict: pass 1 survives

Reasons:
1. **Honesty posture.** The product's hardest rule is comprehension-first measurement. A stopwatch aesthetic rewards the number; a margin rewards the practice. Pass 2's own risk section concedes this.
2. **Ownability.** The margin rail is unowned white space; the lap strip resembles every fitness app's history list.
3. **Calm.** Pass 1 has zero ambient animation; pass 2 needs a pulse it then apologizes for.

## Carry-over list (pass 2 → final, exact)

1. Delta display in log/session rows (`+N wpm` paired with comprehension %, never naked).
2. Terse instrument copy where it beats atelier prose: "Start", "Pause", "Lap" numbering for sessions ("Lap 12" as the session row title).
3. 150ms response option noted; final timing decided in pass 3 (lean 180ms calm over 150ms snap unless convergence says otherwise).
4. Tabular-nums everywhere (already in pass 1 — confirmed, not added).

Killed ideas stay dead: readout hero, LIVE pulse, signal-orange ORP, side-railless layout as default (single column remains the <900px rule only).

# 11: Publish implementation tickets

**Category:** enhancement

**What to build:** A to-tickets round over the final spec (09) plus the architecture decision (10): vertical implementation slices with a dependency matrix, triaged, published under `.scratch/speedreading-redesign-build/issues/`.

**Blocked by:** 10 architecture-review.

**Status:** ready-for-agent

- [x] Slices are vertical (each demoable), sized for one fresh context, with blocking edges
- [x] Dependency matrix recorded (map or matrix section)
- [x] Each ticket triaged (category, state, redundancy check, brief where needed)
- [x] Unit + e2e green is an explicit acceptance item on the final slice

## Resolution

Delivered: `.scratch/speedreading-redesign-build/` — map (with dependency matrix), spec, 10 tickets (B1–B3 frontier-parallel, S1–S7 sequential chain with cross edges), each triaged with category/state/redundancy/brief. Frontier: B1, B2, B3.

## Comments

> *This was generated by AI during triage.*

Triage: enhancement; meta-ticket executed after 10. State: ready-for-agent.

# 01: Chapter picker

**Category:** enhancement

**What to build:** A reader can move between chapters of a multi-chapter text instead of only reading the first substantial chapter. From the library or player, the reader sees the chapter list, jumps to any chapter, and sessions record which chapter was read.

**Blocked by:** None (can start immediately).

**Status:** ready-for-agent

- [x] Chapter list is reachable from an open text and shows every chapter title
- [x] Jumping to a chapter restarts RSVP playback at that chapter with correct word counts
- [x] Session records identify the chapter read; dashboard stays correct for multi-chapter texts
- [x] `node --test` green; no ADR contradiction (ADR-7 keyboard/a11y rules apply to the new control)

## Comments

> *This was generated by AI during triage.*

Triage: category enhancement (new capability, nothing broken). Redundancy check: no chapter UI exists — the composition root always opens the first chapter with 50+ words and no view lists chapters (verified by code search). Prior rejections: none (no `.out-of-scope/` directory). No grilling needed: scope is a single control plus session attribution. State: ready-for-agent.

## Agent Brief

**Category:** enhancement
**Summary:** Add chapter navigation so multi-chapter texts (e.g. a 16-chapter EPUB) are fully readable, not just the first substantial chapter.

**Current behavior:**
Opening a text always starts playback at the first chapter with 50+ words. There is no UI listing chapters and no way to reach later chapters; the chapter title shown comes from whichever chapter was auto-picked.

**Desired behavior:**
The reader can open a chapter list for the current text, pick any chapter, and playback restarts there with that chapter's title and word count. The recorded session attributes the chapter read so per-session WPM/comprehension stay meaningful. Keyboard and screen-reader behavior follows the existing player conventions (real buttons, focus outline, announcements only at sentence boundaries).

**Key interfaces:**
- The text record's `chapters` array (index/title/text/wordCount entries): read-only source for the list
- The session record: must carry which chapter was read alongside text id, chunk size, WPM, and comprehension
- The player view's start contract (player + text object) and settings flow: reused unchanged; only the chapter fed into chunking changes

**Acceptance criteria:**
- [x] Chapter list shows every chapter title of a 3+ chapter text, keyboard-navigable
- [x] Selecting a chapter restarts RSVP from its first chunk with its title and correct total
- [x] Session row and quiz associate with the chapter actually read
- [x] Single-chapter texts behave exactly as before (no list shown or list with one entry)
- [x] `node --test` green

**Out of scope:**
- Bookmarks or resume-position memory across visits
- Reordering, renaming, or deleting chapters
- Changing the chunking, ORP, quiz, or storage designs

## Resolution

Delivered 2026-09-23 (M-F01). Library chapter select; explicit index wins; session + quiz carry chapterIndex/chapterTitle (quiz persistence added after review blocker). E2E: 'chapter-picker: explicit chapter selection opens Chapter Two', 'session carries chapter attribution', 'legacy unattributed session still renders'. Review: reviews/M-F01-r2.md APPROVED. Suite 109/109; walkthrough 39/39.

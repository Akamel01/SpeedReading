# 06: Screen-reader and keyboard-only verification

**Category:** enhancement

**What to build:** A human verifies the full loop (import, play, quiz, dashboard) keyboard-only and with a real screen reader, closing the accessibility evidence gap that automation cannot cover, and files fixes for whatever it finds.

**Blocked by:** None (can start immediately).

**Status:** ready-for-human

- [ ] Full loop completed keyboard-only with no pointer, noting every trap or dead end
- [ ] Full loop completed with a real screen reader (e.g. VoiceOver/NVDA), noting announcement quality
- [ ] Findings filed as follow-up tickets or fixed inline with before/after notes
- [ ] Reduced-motion manual sentence mode included in both passes

## Comments

> *This was generated by AI during triage.*

Triage: category enhancement (verification depth). Redundancy check: automation covers ARIA attributes, focus outline, contrast ratio, and reduced-motion defaults, but no human pass with assistive technology exists (verified). This needs human judgment and external tooling, so it cannot be delegated to an AFK agent. State: ready-for-human.

## Agent Brief

**Category:** enhancement
**Summary:** Human-only verification pass: run the whole app keyboard-only and with a real screen reader; report or fix what automation missed.

**Current behavior:**
The app exposes keyboard shortcuts, focus outlines, a hidden visual stage with sentence-boundary announcements, and a reduced-motion manual sentence mode. Automated checks assert the attributes, the contrast ratio, and the reduced-motion default, but nobody has listened to the announcements or tabbed the full loop.

**Desired behavior:**
A human completes import → baseline → RSVP play → quiz → dashboard → reload using keyboard only, then repeats the loop with a screen reader running, including the reduced-motion manual sentence mode. Every trap, silent control, confusing announcement, or focus loss is recorded; trivial fixes land inline, the rest become follow-up tickets.

**Key interfaces:**
- The existing keyboard map, live region, manual sentence controls, and view routing: under test, not under redesign
- The accessibility primitives module (announcements, focus helpers, reduced-motion detection): fix target if announcements misbehave

**Acceptance criteria:**
- [ ] Keyboard-only pass documented step by step with traps listed (or "none found")
- [ ] Screen-reader pass documented with announcement quality notes
- [ ] Reduced-motion manual mode covered in both passes
- [ ] Each finding is either fixed (with before/after note) or filed as a follow-up ticket

**Out of scope:**
- Automated screen-reader testing frameworks
- Redesigning the visual RSVP presentation
- Changing the announcement strategy without evidence from the pass

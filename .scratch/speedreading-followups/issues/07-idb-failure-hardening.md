# 07: IndexedDB failure-path hardening

**Category:** enhancement

**What to build:** Storage failures (quota exceeded, aborted transactions, corrupt import payloads) surface as readable, recoverable UI states instead of silent drops or console-only errors, proven by simulated-failure tests.

**Blocked by:** None (can start immediately).

**Status:** ready-for-agent

- [x] Quota-exceeded write rejects with a readable message and the UI tells the reader what to do
- [x] Aborted transactions reject (no hangs, no half-written state); import validates before clearing
- [x] Simulated-failure tests cover quota, abort, and malformed import payloads
- [x] `node --test` green

## Comments

> *This was generated by AI during triage.*

Triage: category enhancement (robustness). Redundancy check: the storage wrapper rejects readably on quota/abort and import validates before clearing, but no test exercises these paths in a real browser (verified by code search). Prior rejections: none. State: ready-for-agent.

## Agent Brief

**Category:** enhancement
**Summary:** Harden storage failure paths with simulated quota/abort tests and readable, recoverable UI states.

**Current behavior:**
Writes reject with readable errors on quota/abort instead of resolving early, and import validates the snapshot before touching existing data. Session-save and import failures already surface alerts. None of this is exercised under simulated failure — it is correct by inspection.

**Desired behavior:**
A test harness simulates quota-exceeded writes, aborted transactions, and malformed import payloads against the real storage wrapper in a browser context, asserting each failure rejects readably, leaves no half-written state, and reaches the user as an actionable message (free space, retry, or re-export guidance — not a stack trace).

**Key interfaces:**
- The store contract (put/get/delete/export/import with documented error objects): behavior under test
- The existing alert surfaces on the import and session-save paths: reuse, don't invent new dialogs
- The zero-dependency test runner for pure logic plus the browser walkthrough harness for browser-only paths

**Acceptance criteria:**
- [x] Simulated quota failure: readable rejection, actionable UI message, existing data intact
- [x] Simulated abort: promise rejects (never hangs), no partial writes
- [x] Malformed import payload: documented error object, existing data intact
- [x] `node --test` green; browser evidence recorded

**Out of scope:**
- Cloud backup or sync (contradicts ADR-10)
- Changing the export schema (see ticket 05)
- Performance work (see ticket 08)

## Resolution

Delivered 2026-09-23 (M-F07). scripts/idb-failure.js (real store in Chromium): quota rejection readable + data intact + recovery; clone-failure rejection; malformed imports documented errors; UI alert path. 5/5. Review: reviews/M-F07.md APPROVED_WITH_NOTES (commit-time tx branches recorded as known limit).

# 07: URL import with paste fallback

**Category:** enhancement

**What to build:** A reader pastes an article URL; the app fetches it, extracts the readable article text, and imports it — or, on CORS/network failure, says so plainly and offers the paste box instead.

**Blocked by:** None — decision made: direct fetch + readable-text extraction + paste fallback (no proxy).

**Status:** ready-for-agent

- [ ] URL input in the library; fetch with same-origin/CORS respected (no proxy, no backend)
- [ ] Article extraction strips scripts/styles/nav/chrome, prefers `<article>`/main content
- [ ] Failure shows a readable message and reveals the paste box path
- [ ] Explicit user-initiated fetch only; posture documented in README
- [ ] `node --test` green with fixture HTML cases

## Comments

> *This was generated by AI during triage.*

Triage: category enhancement. Redundancy: no fetch path exists anywhere in `src/` (verified). State: ready-for-agent.

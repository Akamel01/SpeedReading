# 03: Paste-text import

**Category:** enhancement

**What to build:** A reader pastes text into a box in the library and imports it as a first-class text (chapters, quiz, sessions, export all work).

**Blocked by:** None (can start immediately). Supersedes `speedreading-followups/09`.

**Status:** ready-for-agent

- [ ] Paste box + import button in the library; empty paste rejected readably
- [ ] Pasted text flows through the same ingest pipeline (title default + chapters + word count)
- [ ] Downstream identical to file imports
- [ ] `node --test` green

## Comments

> *This was generated by AI during triage.*

Triage: category enhancement. Redundancy: file-object import only, no paste path (verified). State: ready-for-agent.

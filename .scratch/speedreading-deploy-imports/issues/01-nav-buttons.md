# 01: Nav buttons route views

**Category:** bug

**What to build:** The four header buttons (Library, Player, Quiz, Dashboard) actually switch views instead of doing nothing. Player/Quiz without active content fall back to Library.

**Blocked by:** None (can start immediately).

**Status:** ready-for-agent

- [ ] Each header button routes to its view; exactly one section visible after click
- [ ] Player/Quiz buttons with no active text/quiz fall back to Library (no dead screen)
- [ ] Keyboard focus moves to the newly shown view
- [ ] `node --test` green

## Comments

> *This was generated by AI during triage.*

Triage: category bug — verified: the four `<button>` elements in the shell header have no handlers and the composition root never touches them. Redundancy: no routing code exists outside per-view `hidden` toggling. State: ready-for-agent.
